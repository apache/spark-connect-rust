# Known environmental failures in the official Spark Connect test suite - tests that
# fail in the CI environment for reasons OUTSIDE our client, deselected so the gate
# tracks genuine client regressions only. Each line is
# `<relpath>::Class::method  # <reason>` (relpath relative to <spark>/python).
# run_official_tests.py's drift check re-runs these every run and reports any that
# start passing, so the list cannot silently rot. Regenerate with
# scripts/gen_parity_skiplist.py on a version bump.
#
# NB: many more tests fail against an *older local* Spark dist that lacks the TIME
# type (UNSUPPORTED_TIME_TYPE); those pass in CI, so they are intentionally NOT
# listed here - the CI drift check confirmed it. Keep this list to the CI reality.
#
# The former arrow-UDF entry (ArrowPythonUDFParityLegacyTests::
# test_type_coercion_string_to_numeric) was REMOVED for 4.3.0: the drift check reports
# it now passes on the 4.3.0-rc1 CI image, so the gate exercises it for real.
pyspark/sql/tests/connect/test_parity_dataframe.py::DataFrameParityTests::test_metadata_column  # requires InMemoryCatalog for 'testcat' - a test-only catalog class absent from the Spark distribution
